extends ../layouts/main-layout.pug

block content
    if orders.length > 0
        .accordion#accordionOrders
            each order, index in orders
                .card.mb-2
                    .card-header(id=`heading${index}`)
                        h2.mb-0
                            button.btn.btn-link.btn-block.text-left(type="button", data-toggle="collapse", data-target=`#collapse${index}`, aria-expanded=(index == 0 ? "true" : "false"), aria-controls=`collapse${index}`)
                                | Order Id: #{order._id}

                    .collapse(id=`collapse${index}`, class=(index == 0 ? "show" : ""), aria-labelledby=`heading${index}`, data-parent="#accordionOrders")
                        .card-body
                            table.table.table-sm.table-bordered
                                thead.thead-dark
                                    tr 
                                        th.text-center Image 
                                        th Product Name
                                        th.text-center Price
                                        th.text-center Quantity 
                                tbody
                                    each product in order.items 
                                        tr 
                                            td.text-center.align-middle
                                                img(src=`/img/${product.image}`, alt=`${product.name}`, style='width: 80px;')
                                            td.align-middle #{product.name}
                                            td.text-center.align-middle #{parseFloat(product.price).toFixed(2)} TL
                                            td.text-center.align-middle #{product.quantity}

                                - var total = 0;
                                each product in order.items
                                    - total += (product.price * product.quantity)
                                tfoot
                                    tr 
                                        td
                                        td(colspan="1").align-middle Sipariş Tarihi #{order.date}
                                        td.text-center.align-middle Toplam:
                                            br
                                            span #{total.toFixed(2)} TL
                                        td
    else 
        h1 No Orders
