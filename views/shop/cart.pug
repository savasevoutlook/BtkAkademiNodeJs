extends ../layouts/main-layout.pug

block content
    
    if products.length > 0
        table.table.table-bordered 
            thead
                tr
                    th(style="width: 100px").text-center Image 
                    th Product Name 
                    th(style="width: 150px").text-center Price 
                    th(style="width: 120px").text-center Quantity
                    th.text-center Actions
            tbody
                each product in products 
                    tr 
                        td.text-center.align-middle
                            img(src=`/${product.productId.imageUrls[0]}`, alt=`${product.productId.name}`, style='width: 100px;')
                        td.align-middle #{product.productId.name}
                        td.text-center.align-middle #{product.productId.price} TL
                        td.text-center.align-middle #{product.quantity}
                        td.text-center.align-middle
                            form(action='/delete-cartitem', method='post')
                                input(type='hidden', name='productId', value=`${product.productId._id}`)
                                input(type="hidden", name="_csrf", value=csrfToken)
                                button(type='submit').btn.btn-outline-danger.btn-sm
                                    i.fa.fa-times
            tfoot
                tr 
                    td(colspan="4")
                        - var total = 0;
                        each product in products
                            - total += (product.productId.price * product.quantity)
                    td.text-center #{total} TL
        .text-center 
            a(href="/").btn.btn-primary.inline-block Alışverişe Devam Et
            form(action="/create-order", method="POST").inline-block
                input(type="hidden", name="_csrf", value=csrfToken)
                button(type="submit").btn.btn-primary.ml-2 Alışverişi Tamamla
    else
        .col-md-12
            h2 No Products Available
